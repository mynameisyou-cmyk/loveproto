# LoveProto Kingdom — Cloudflare Worker

Deploy in 30 seconds:
```bash
npx wrangler deploy
```

Free: 100k requests/day, always free, global edge.

Endpoints:
- / — kingdom landing page
- /wake — the WAKE
- /citizens — citizen list
- /invite — invitation to all
- /status — kingdom status
